import { Box, Typography } from '@mui/material';
import React from 'react';

const Curriculum: React.FC = () => {

   return (
      <Box>
         <Typography>
            I think it would be nice to post the current curriculum here.
         </Typography>
      </Box>
   );
};

export default Curriculum;